@using R10.Web.Areas.Shared.ViewModels.ReportScheduler
@model ScheduleViewModel
@inject IStringLocalizer<SharedResource> Localizer
@{
    var taskIdParam = $"{{taskId: {Model.TaskId}}}";
    //var permission = Model.Permission;
    bool addMode = Model.TaskId == 0;
}

@(Html.Kendo().Grid<ScheduleViewModel>()
                .Name("scheduleGrid")
                .Columns(columns =>
                {
                    //columns.Bound(p => p.NextRunTime)
                    //    .ClientTemplate($"<i class='fal fa-grip-lines sort-handler owner-item-handler' title='{Localizer["Sort"]}'></i>")
                    //    .ClientHeaderTemplate("&nbsp;")
                    //    .Width(50);
                    columns.Bound(p => p.Schedule)
                        //.ClientTemplate("#=Name#")
                        //.Editable("inventionPage.isGridColumnEditable")
                        //.Width(150)
                        //.EditorTemplateName("ComboBox")
                        //.EditorViewData(new
                        //{
                        //    dataTextField = "Name",
                        //    dataValueField = "TaskId",
                        //    action = "GetSchedulesList",
                        //    controller = "ReportScheduler",
                        //    routeValues = new { area = "Shared", property = "Name" },
                        //    template = ComboBoxHelper.GetItemTemplate("OwnerCode", "OwnerName"),
                        //    headerTemplate = ComboBoxHelper.GetHeaderTemplate(ViewData["OwnerLabel"].ToString(), ViewData["OwnerNameLabel"].ToString()),
                        //    onSelect = "function(e) { pageHelper.refreshGridNameField(e, 'OwnerName'); }"
                        //})
                        .HtmlAttributes(new { @class = "name-field" });
                    columns.Bound(p => p.Status)
                        .HtmlAttributes(new { @class = "name-field" });
                    //.HtmlAttributes(new { @class = "d-none d-sm-table-cell name-field" })
                    //.HeaderHtmlAttributes(new { @class = "d-none d-sm-table-cell" });
                    //columns.Command(command =>
                    //{
                    //    if (permission.CanDeleteRecord)
                    //        command.Custom("Delete")
                    //                .Click("function(e) {inventionPage.deleteGridRow(e,this)}")
                    //                .IconClass("k-icon fal fa-trash-alt")
                    //                .Text($"<span class='grid-action-label'>{Localizer["Delete"]}</span>")
                    //                .HtmlAttributes(new { @class = "grid-action" });

                    //}).Width(10);
                })
                .Selectable(selectable => selectable.Mode(GridSelectionMode.Single))
                //.ToolBar(toolbar =>
                //{
                //    if (permission.CanAddRecord & !addMode)
                //    {
                //        toolbar.Create().IconClass("k-icon fal fa-plus").Text(Localizer["Add"]).HtmlAttributes(new { @class = "grid-action" });
                //    }
                //    if (permission.CanEditRecord && !permission.CanEditRemarksOnly)
                //    {
                //        toolbar.Custom().Name("Save").IconClass("k-icon fal fa-save").Text(Localizer["Save"]).HtmlAttributes(new { @class = "d-none" });
                //        toolbar.Custom().Name("Cancel").IconClass("k-icon fal fa-times").Text(Localizer["Cancel"]).HtmlAttributes(new { @class = "d-none" });
                //    }

                //})
                //.Editable(editable => editable.Enabled(permission.CanEditRecord && !permission.CanEditRemarksOnly)
                //                .Mode(GridEditMode.InCell).
                //                ConfirmDelete(@Localizer["Are you sure you want to delete this record?"]).DisplayDeleteConfirmation(false))

                //.Pageable()
                //.AutoBind(false)
                .DataSource(dataSource => dataSource
                    .Ajax()
                    .Batch(true)
                    //.PageSize(10)
                    //.ServerOperation(false)
                    .Model(model =>
                    {
                        model.Id(s => s.TaskId);
                        model.Field(s => s.Schedule).Editable(false);
                        model.Field(s => s.Status).Editable(false);
                        model.Field(s => s.Trigger).Editable(false);
                        model.Field(s => s.NextRunTime).Editable(false);
                        model.Field(s => s.CreatedBy).Editable(false);
                        model.Field(s => s.DateCreated).Editable(false);
                        model.Field(s => s.UserId).Editable(false);
                    })
                    .Read(read => read.Action("GetSchedulesList", "ReportScheduler"))
                    //.Update(update => update.Action("GridUpdate", "PatOwnerInv"))
                    //.Destroy(update => update.Action("GridDelete", "PatOwnerInv"))
                    .Events(events =>
                    {
                        events.Error("pageHelper.onGridError");
                    })
                )
                //.Events(events =>
                //{
                //    events.DataBound("inventionPage.setGridReadOnlyRow");
                //})
                .HtmlAttributes(new { @class = "k-grid-border" })
)

@*@(Html.Kendo().Sortable()
                        .For("#inventionOwnerGrid")
                        .Filter("table > tbody > tr:not(.k-grid-edit-row)")
                        .Cursor("grabbing")
                        .Handler(".owner-item-handler")
                        .HintHandler("pageHelper.hint")
                        .PlaceholderHandler("pageHelper.placeholder")
                        .ContainerSelector("#inventionOwnerGrid tbody")
                        .Events(events => events.Change($"function (e) {{ inventionPage.onGridReorder(e, '{Url.Action("GridReorder", "PatOwnerInv")}') }}"))
)*@
