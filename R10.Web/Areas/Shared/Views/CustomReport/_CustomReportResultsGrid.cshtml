@model CustomReportPageViewModel
@inject IStringLocalizer<SharedResource> Localizer

@{
    var resultLabel = Localizer["Select the query below that you want to view or modify."];
    var gridName = $"{Model.PageId}-Grid";
}

<div class="pl-2" style="font-size:small; font-style:italic;">
    @resultLabel

</div>
@(Html.Kendo().Grid<CustomReportSearchResultViewModel>()
    .Name(gridName)
    .Columns(columns =>
    {
        columns.Bound(q => q.ReportName).Title(Localizer["Report Name"]).HtmlAttributes(new { @class = "cut-off" });
    })
    //.ToolBar(tools => tools.ClientTemplate(Model.ToolbarTemplate(Url.Action("Add", new { fromSearch = true }), Localizer["Add"], Localizer["Export to Excel"])))
    .AutoBind(false)
    .Pageable(page => page.PageSizes(true).Refresh(true))
    .Sortable(sort => sort.AllowUnsort(false))
    .Scrollable()
    .DataSource(dataSource => dataSource
                    .Ajax()
                    .Model(model =>
                    {
                        model.Id(cr => cr.ReportId);
                    })
                    .Events(events => events.Error("customReport.searchResultGridError"))
                    .Events(events => events.RequestEnd("customReport.searchResultGridRequestEnd"))

                    .PageSize(Model.GridPageSize)
                    .Read(read => read.Action("PageRead", "CustomReport")
                    .Data("customReport.gridMainSearchFilters"))
                    .Sort(sort => sort.Add(i => i.ReportName))
                )
    .Selectable()
    .Events(events => events
        .Change("customReport.gridDisplayDetail")
        .DataBound("customReport.searchGridSelectRow")
        )

    .HtmlAttributes(new { @class = "kendo-Grid" })
)

<style>
    #@gridName tr:hover {
        cursor:pointer;
    }
    #@(gridName) tr.k-state-selected {
        background-color: var(--grid-hover) !important;
    }
     #@(gridName) .k-grid-content {
        min-height: 100px;
        max-height: 450px;
    }
    .cut-off {
        overflow: hidden;
        white-space: nowrap;
        text-overflow: ellipsis;
    }
</style>
