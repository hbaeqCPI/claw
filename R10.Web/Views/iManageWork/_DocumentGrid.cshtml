@model DocumentViewerViewModel
@inject IStringLocalizer<SharedResource> Localizer
@inject ISystemSettings<DefaultSetting> Settings
@{
    var settings = await Settings.GetSetting();
    var permission = Model.Permission ?? new DocumentViewerPermission();
    var viewerName = $"iManageDocumentGrid_{Model.PageId}";
    var showDocumentLink = permission.CanDownload;
}
<div class="results grid" data-name="@viewerName">
    @(
        Html.Kendo().Grid<DocumentViewModel>()
            .Name(viewerName)
            .Columns(columns =>
            {
                columns.Template("<span><input type='checkbox' data-document-id='#:Id#' draggable='true' ondragstart='iManage.documentOnDragStart(event)'/><i class='far fa-grip-dots-vertical'></i></span>").HeaderHtmlAttributes(new { @class = "grab" }).HtmlAttributes(new { @class = "grab" });
                columns.Select().HeaderHtmlAttributes(new { @class = "select"}).HtmlAttributes(new { @class = "select" }).Width(50);
                columns.Bound(d => d.Type).ClientTemplate("<i class='#:IconClass# fa-fixed-width'></i>").ClientHeaderTemplate("<i class='fal fa-file fa-fixed-width'></i>");
                if (showDocumentLink) 
                {
                    columns.Bound(d => d.Title).Title(Localizer["Title"].ToString()).ClientTemplate("<a href='##' onclick='iManage.documentOnClick(event)' data-document-id='#:Id#'' rel='noopener' draggable='false'>#:Title#</a>").HtmlAttributes(new { @class = "view-details-icon" });
                }
                else 
                {
                    columns.Bound(d => d.Title).Title(Localizer["Title"].ToString());
                }
                columns.Bound(d => d.ContainerName).Title(Localizer["Folder"].ToString());
                columns.Bound(d => d.Version).Title(Localizer["Version"].ToString());
                columns.Bound(d => d.EditDate).Title(Localizer["Edit Date"].ToString()).ClientTemplate("#: (EditDate == null) ? '' : kendo.toString(EditDate,'dd-MMM-yyyy h:mm tt')#");                
                columns.Bound(d => d.LastUser).Title(Localizer["Last User"].ToString());
                columns.Bound(d => d.Size).Title(Localizer["File Size"].ToString()).ClientTemplate("#:pageHelper.formatFileSize(Size)#");
                columns.Command(command => {
                    command.Custom("Open").Text(" ").Click("iManage.gridOnCommand").IconClass("k-icon fal fa-arrow-up-right-from-square fa-fixed-width").HtmlAttributes(new { title = Localizer["Open"], draggable = false });
                    if (permission.CanDownload)
                    {
                        command.Custom("Download").Text(" ").Click("iManage.gridOnCommand").IconClass("k-icon fal fa-download fa-fixed-width").HtmlAttributes(new { title = Localizer["Download"], draggable = false });
                    }
                    if (permission.CanEdit)
                    {
                        command.Custom("Edit").Text(" ").Click("iManage.gridOnCommand").IconClass("k-icon fal fa-pencil fa-fixed-width").HtmlAttributes(new { title = Localizer["Edit"], draggable = false });
                    }
                    if (permission.CanDelete)
                    {
                        command.Custom("Delete").Text(" ").Click("iManage.gridOnCommand").IconClass("k-icon fal fa-trash-alt fa-fixed-width").HtmlAttributes(new { title = Localizer["Delete"], draggable = false });
                    }
                }).HtmlAttributes(new { @class = "text-right" });
            })
            .Sortable(sort => sort.AllowUnsort(false))
            .Selectable(selectable => selectable.Mode(GridSelectionMode.Multiple))
            .Pageable(pageable => pageable.Refresh(true).PageSizes(true).Responsive(false))
            .DataSource(dataSource => dataSource
                .Ajax()
                .ServerOperation(false)
                .PageSize(settings.SubFormPageSize)
                .Model(model =>
                {
                    model.Id(d => d.Id);
                })
                .Sort(sort => sort.Add(d => d.Title))
                .Read(read => read.Action("ViewerRead", "iManageWork", new { Area = "" }).Data("iManage.viewerReadData"))
                .Events(events =>
                {
                    events.Error("iManage.viewerOnError");
                    events.RequestEnd("iManage.viewerOnRequestEnd");
                })
            )
            .Events(events => {
                events.Change("iManage.viewerOnChange");
                events.DataBound("iManage.viewerOnDataBound");
            })
    )
</div>