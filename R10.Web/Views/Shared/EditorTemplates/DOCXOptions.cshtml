@inject IStringLocalizer<SharedResource> Localizer
@*@model DOCXOptionViewModel*@
@model int

    <script>
        function updateSelectRowGenAllDOCXesDescription(e) {
            var comboBox = e.sender;

            if (comboBox.selectedIndex >= 0) {
                const name = comboBox.dataItem()["Description"];
                @Html.Raw((ViewData["onSelect"].ToString() + "('GenAllDOCXesDescription',name);"))
            }
        }
    </script>

@(Html.Kendo().DropDownListFor(m => m)
        .DataValueField("GenAllDOCXes")
        .DataTextField("Description")
        .Events(e => e.DataBound("function () {  if (this.select() === -1) {  this.select(0);  this.trigger('change'); }}"))
        .Events(e => e.Change("updateSelectRowGenAllDOCXesDescription"))
        .BindTo(DOCXOptionViewModel.BuildList(Localizer))
)
