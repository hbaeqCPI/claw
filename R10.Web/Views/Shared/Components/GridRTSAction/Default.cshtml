@model GridOptions
@inject IStringLocalizer<SharedResource> Localizer
@{
    var actionColumnDetailTemplate = "<span>#: SearchAction#</span><br><span>#: PMSAction#</span>";
}
<div class="row">
    <div class="col-3 mx-auto">
        <div class="form-group float-label">
            <label>@Localizer["Display"]</label>
            @(Html.Kendo().DropDownList()
                  .Name("ActionDisplayOption")
                  .DataTextField("Text")
                  .DataValueField("Value")
                  .BindTo(new List<SelectListItem>()
                  {
                  new SelectListItem()
                  {
                      Text = Localizer["Patent Office Actions (as downloaded)"],
                      Value = "0"
                  },
                  new SelectListItem()
                  {
                      Text = Localizer["Patent Office Actions (as matched)"],
                      Value = "1"
                  }
                  })
                  .Value("0")
                  .Events(events => events.Select($"function(e) {{patCountryAppPage.rtsActionDisplaySelect(e, '{Model.ParentScreen}')}}"))
                  .HtmlAttributes(new { style = "width: 100%", id = $"rtsPublicDataTabAction_ActionDisplayOption_{Model.ParentScreen}" })
            )
        </div>
    </div>
</div>
<div class="col-md-12">
    @(Html.Kendo().Grid<RTSSearchActionAsDownloadedDTO>().Name($"rtsActionAsDownloadedGrid_{Model.ParentScreen}")
                      .Columns(columns =>
                      {
                          columns.Bound(a => a.SearchAction);
                          columns.Bound(a => a.BaseDate).Format("{0:dd-MMM-yyyy}");

                      })
        .Pageable(page => page.Responsive(false))
                      .Sortable(sort => sort.AllowUnsort(false))
                      .AutoBind(true)
                      .DataSource(dataSource => dataSource
                          .Ajax()
                          .Read(create => create.Action("GetActionsAsDownloadedData", "RTSInfo", new { Area = "Patent", plAppId = Model.ParentId }))
                          .ServerOperation(false)
                          .Events(events =>
                          {
                              events.Error("pageHelper.onGridError");

                          })
                      )
                      .HtmlAttributes(new { @class = "kendo-Grid" })
        )
</div>
<div class="col-md-12">
    @(Html.Kendo().Grid<RTSSearchActionAsDownloadedDTO>().Name($"rtsActionAsMatchedGrid_{Model.ParentScreen}")
                      .Columns(columns =>
                      {
                          columns.Bound(a => a.SearchAction).ClientTemplate(actionColumnDetailTemplate);
                          columns.Bound(a => a.BaseDate).Format("{0:dd-MMM-yyyy}");
                          columns.Bound(a => a.DueDate).Format("{0:dd-MMM-yyyy}");
                          columns.Bound(a => a.DateCompleted).Format("{0:dd-MMM-yyyy}");
                      })
        .Pageable(page => page.Responsive(false))
                      .Sortable(sort => sort.AllowUnsort(false))
                      .AutoBind(false)
                      .DataSource(dataSource => dataSource
                          .Ajax()
                          .Read(create => create.Action("GetActionsAsMatchedData", "RTSInfo", new { Area = "Patent",plAppId=Model.ParentId }))
                          .ServerOperation(false)
                          .Events(events =>
                          {
                              events.Error("pageHelper.onGridError");
                          })
                      )
                      .HtmlAttributes(new { @class = "kendo-Grid", @style="display:none"})
        )
</div>
